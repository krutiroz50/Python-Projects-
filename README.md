# Python-Projects-
The program picks a secret word and saves it as a string. The user keeps guessing in a loop, and each guess is checked using string comparison. Based on matching letters and their positions, the program gives hints until the correct word is guessed
